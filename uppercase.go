package main

import "strings"

// Upper converts strings to their upper case equivalent
// using built in strings.ToUpper, so don't ask me how it works
func Upper(val string) string {
	return strings.ToUpper(val)
}
